# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  activerecord:
      attributes:
        active_record_log:
          ar_id:                       ActiveRecord id
          ar_table_name:               ActiveRecord table name
        cbrain_task:
          cluster_jobid:               Job id on cluster
          cluster_workdir:             Working directory on cluster
          share_wd_tid:                Working directory is shared with task
          cluster_workdir_size:        Working directory size on cluster
          workdir_archive_userfile_id: Working directory archive
        data_provider:
          remote_dir:                  Remote directory
        meta_data_store:
          ar_id:                       ActiveRecord id
          ar_table_name:               ActiveRecord table name
          meta_key:                    Metadata key
          meta_value:                  Metadata value
        remote_resource:
          actres_user:                 ActiveResource username
          actres_host:                 ActiveResource hostname
          actres_port:                 ActiveResource port
          actres_dir:                  ActiveResource directory prefix
          ssh_control_rails_dir:       Rails root directory
          tunnel_mysql_port:           Port for MySQL tunnel
          tunnel_actres_port:          Port for ActiveResource tunnel
          dp_cache_dir:                Data provider cache directory
          dp_ignore_patterns:          Filename ignore patterns
          spaced_dp_ignore_patterns:   Filename ignore patterns
          cms_extra_qsub_args:         Extra qsub options
          cms_shared_dir:              CMS shared directory
          workers_chk_time:            Workers check interval
          rr_timeout:                  Remote resource timeout
        tool_config:
          env_array:                   Environment variables array
          ncpus:                       Number of CPUs
        tool:
          select_menu_text:            Text for select box
        userfile:
          num_files:                   Number of files in collection
        user:
          email:                       Email
        signup:
          first:                       First name
          last:                        Last name
