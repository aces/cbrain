This is automated message from <%= the_organization %>.

Your account for the service:

  <%= @demand.service %>

was approved and created.
<% if @url.present? %>

The main login page for the service is at

  <%= @url.to_s %>
<% end %>

Your login name is: <%= @demand.login %> .

<% if @plain_password.present? %>
Your temporary password is: <%= @plain_password %>

You will be asked to reset it once you log in.
<% else %>
A temporary password has been set, and you'll have to
use the "Lost password" feature to reset it.
<% end %>
Use a solid password, and DO NOT reuse a password from
any other service! (This is true for ALL internet
services, you know?)

Thank you.
