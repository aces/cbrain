<%= button_with_dropdown_menu("View Options") do %>
  <div class="view_options">
     Number of users per page:
     <%= ajax_onchange_select( :per_page, users_path(:update_filter => true), options_for_select( [ 25, 50, 100, 200, 500 ], @users_per_page ), :datatype => "script") %>
  </div>
<% end %>
