
<% ajax_form_for :tool, @tool, :url => { :action => "create" }, :datatype => "script" do |f| -%>

  <%= render :partial => 'form_fields', :locals  => {:f  => f} %>
  <p><%= f.submit "Create new tool" %></p>

<% end %>

