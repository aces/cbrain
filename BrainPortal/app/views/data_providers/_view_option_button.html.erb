<%= button_with_dropdown_menu("View Options") do %>
  <div class="view_options">

    <%= ajax_link 'Refresh list of files', browse_data_provider_path(:refresh => true, :per_page=> params[:per_page] ), :datatype  => 'script' %><br/>
  
    Number of files per page:
    <%= ajax_onchange_select( :per_page, browse_data_provider_path, options_for_select( [25, 50, 100, 200, 500 ], @per_page ), :datatype => "script") %>
    
  </div>
<% end %>

