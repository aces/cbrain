
Upload a single file to project <%= @nh_project.name %>
<hr>

<%= form_tag upload_file_nh_project_path(@nh_project), :multipart => true do %>

  <%= label_tag :upload_file, "Select file to upload:" %>
  <%= file_field_tag :upload_file %>
  <hr>
  <%= label_tag :nh_dp_id, "Select storage for file content:" %>
  <%= data_provider_select(:nh_dp_id,
      { :data_providers => @nh_dps },
      { :include_blank  => '(Select storage)' } )
  %>
  <hr>
  <%= label_tag :nh_project_id, "Optional: assign file to a different project:" %>
  <%= group_select(:file_nh_project_id,
      { :groups => @nh_projects, :selector => @nh_project.id },
      { :include_blank => '(Use project of selected storage)' } )
  %>
  <hr>
  <%= submit_tag "Upload the file!" %>

<% end %>

<hr>
More advanced upload options are available in the
<%= button_to "CBRAIN file manager for this project",
    switch_groups_path(id: @nh_project.id),
    :class => "btn-solid-secondary nh_action",
    :method => :post
%>
