
<%
  # This partial displays an object's log
  # It requires two args, the 'log' and a 'title'
%>

<% unless log.blank? %>
  <table class="log_table">
    <tr>
      <th><%= title.blank? ? "Processing Log" : h(title) %></th>
    </tr>
    <tr>
      <td><pre><%= Message.parse_markup(h(log)) %></pre></td>
    </tr>
  </table>
<% end %>

