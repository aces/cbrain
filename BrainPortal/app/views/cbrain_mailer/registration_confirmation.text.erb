
Welcome to CBRAIN and NEUROHUB, <%= @user.full_name %>,

Your account has been set up.

Access CBRAIN here:

    <%= RemoteResource.current_resource.site_url_prefix || "(CBRAIN Site URL Not Configured!)" %>

Access NEUROHUB here:

   <% if RemoteResource.current_resource.site_url_prefix.presence %>
	 <%= File.join(RemoteResource.current_resource.site_url_prefix + "/neurohub") %>
   <% else %>
     <%= "(NEUROHUB Site URL Not Configured!)" %>
   <% end %>

Your username for logging in is: <%= @user.login %>
<% unless @no_password_reset_needed %>
Your temporary password is: <%= @plain_password.html_safe %>

You will be asked to change your password upon first log in.
<% end %>

Sincerely,

The CBRAIN and NEUROHUB administrators.

