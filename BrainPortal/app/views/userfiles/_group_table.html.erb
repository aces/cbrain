<table>

 <% if current_user.has_role?(:admin)%>
	 <%= collection_select(:group, :group_id, @user_groups, :id, :name, options = {:prompt => "-Select a group-"}, :class => :group) %>
  <% else %>
	 <%= collection_select(:group, :group_id, @user_groups, :id, :name, options = {:prompt => "-Select a group-"}, :class => :group) %>
  <% end %>
</table>
