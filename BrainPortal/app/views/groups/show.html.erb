<% title "Project Info" %>

<h2>Project details for <%= h @group.name %></h2>

<div class="generalbox">
  <% if @group.is_a?(WorkGroup) && @group.can_be_edited_by?(current_user)%>
    <div class="menu_bar menu_bar_left">
      <%= link_to 'Switch', switch_group_path(@group), :class => "button", :method  => :post %>
      <%= link_to 'Edit', edit_group_path(@group), { :class => "button" } %>
      <%= link_to 'Delete', group_path(@group), { :confirm => 'Are you sure?', :method => :delete, :class => "button" } %>
    </div>
  <% end %>
  <P>
  <table class= "resource_list">
    <tr>
        <th>Name</th>
        <th>Members</th>
        <th>Site</th>
    </tr>
    <td>
       <%= h(@group.name) %>
    </td>
    <td>
        <%= @group.users.sort{ |a,b| a.login.casecmp(b.login)}.collect{ |m| link_to_user_if_accessible(m)}.join("<br>") %>
    </td>
    <td>
       <%= link_to_site_if_accessible(@group.site) %>
    </td>
    </tr>
  </table>
</div>
