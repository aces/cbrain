<!-- 
   Partial to be rendered for <%= "Drmaa#{class_name}" %>
-->
<%% title '<%= class_name %> Parameters' %>

<h1>New <%= class_name %> task</h1>

<%%
     form_tag( '/tasks/create' ) do 
-%>

<%%= hidden_field_tag :task, '<%= "Drmaa#{class_name}" %>' %>
<%% params[:file_ids].each do |id| %>
<%%= hidden_field_tag 'file_ids[]',  id %>
<%% end %>

<!-- 
   Render form fields here.
   Default arguments returned by <%= "Drmaa#{class_name}" %>.get_default_args()
     are stored in the hash: default_args
-->

<%%= submit_tag 'Start <%= class_name %>' %>

<%% end %>
